apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: fitsphere-backend
spec:
  selector:
    matchLabels:
      monitoring.fitsphere.io/scrape: "true"
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
